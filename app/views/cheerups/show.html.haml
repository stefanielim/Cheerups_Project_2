%h1
=@cheerup.user.name
says:

- if @cheerup.cheerup_image?
  %p= link_to image_tag(@cheerup.cheerup_image.thumb.url), @cheerup.cheerup_image.detail.url
- else
%p= @cheerup.content

%p Posted: #{time_ago_in_words(@cheerup.created_at)} ago